fn fib(n) {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}

let start = now();
let res = fib(25);
let end = now();
println "Fib 25: " + res;
println "Time: " + (end - start) + "ms";
