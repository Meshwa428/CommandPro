let start = now();
fn test() {
    let s1 = "hello_world";
    let s2 = "hello_world";
    let i = 0;
    loop while (i < 1000000) {
        if (s1 == s2) {
            # success
        }
        i = i + 1;
    }
}
test();
let end = now();
println "RESULT:" + (end - start);