let start = now();
# 6. Global Lookup Benchmark
let globalCounter = 0;

fn test() {
    globalCounter = globalCounter + 1;
}

let i = 0;
loop while (i < 1000000) {
    test();
    i = i + 1;
}
let end = now();
println "RESULT:" + (end - start);
