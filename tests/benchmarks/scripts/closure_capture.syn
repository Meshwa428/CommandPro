# @EXPECT SKIP_VM
let start = now();
# 11. Closure Capture Benchmark
fn outer() {
    let x = 10;
    fn inner() {
        return x;
    }
    return inner;
}

let f = outer();
let i = 0;
loop while (i < 1000000) {
    f();
    i = i + 1;
}
let end = now();
println "RESULT:" + (end - start);
