let cache = {}
fn fib_memo(n) {
    if n <= 1 { return n }
    let v = cache[n]
    if v != none { return v }
    let r = fib_memo(n - 1) + fib_memo(n - 2)
    cache[n] = r
    return r
}
print(fib_memo(35))
